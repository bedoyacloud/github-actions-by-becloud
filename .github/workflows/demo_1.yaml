name: Hola BeCloud
on: [push]

jobs:
  nombre_del_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.4
      - name: ls
        run: |
          chmod +x script.sh
          ./script.sh