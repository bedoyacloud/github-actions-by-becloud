name: Hola BeCloud
on: [push]

jobs:
    becloud:
            runs-on: ubuntu-latest

            steps:
            - name: by bedoya
              run: echo "BeCloud and level up your cloud skills by BedoyaCloud"
            
            - name: touch file
              run: touch hola.txt

            - name: Set text
              run: echo "Hola BeCloud" > hola.txt

            - name: cat file
              run: cat hola.txt

    becloud2:
            runs-on: ubuntu-latest
            needs: becloud
            steps:
            - name: checkout
              uses: actions/checkout@v4.2.2
              
            - name: LS By Bedoya
              run: ls -la