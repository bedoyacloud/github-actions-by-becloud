name: Hola BeCloud
on:
  push:
  workflow_dispatch:

jobs:
  nombre_del_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: ls
        uses: ./.github/actions/my-action